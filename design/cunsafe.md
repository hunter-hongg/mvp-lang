# C不安全模型

## 概述
- 允许**内嵌C语言程序**，语法如下：
```mvp
c unsafe foo = /*与普通函数相同的参数和返回值声明 */ => """
  /* C程序 */
"""
```

## 实现方案
- `""" ... """`作为单独`token`：多行原始字符串
- **Codegen**阶段把识别到的内容**直接复制到生成的C程序中**
- 注意：内嵌程序属于`unsafe`函数，受**Semantic阶段**与普通`unsafe`函数相同的**调用检查**
